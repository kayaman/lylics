apiVersion: v1
kind: Service
metadata:
  name: {{ include "lylics.fullname" . }}
  labels:
    {{- include "lylics.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "lylics.selectorLabels" . | nindent 4 }}
